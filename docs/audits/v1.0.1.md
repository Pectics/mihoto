# Mihoto v1.0.1 release audit

This record is updated from observed command and workflow results. It must be
complete before the `v1.0.1` tag is pushed; a placeholder or inherited
v1.0.0 result is not an acceptance result for this candidate.

## Release markers

Tested candidate commit: `47519443f41ab5412be41252a50f9e86ccd56e1c`

Release tag: `v1.0.1`

Package version: `1.0.1`

Hosted systemd/TUN acceptance: PENDING

Release decision: Pending hosted systemd/TUN acceptance

## Automated gates

For candidate commit `47519443f41ab5412be41252a50f9e86ccd56e1c`, the local
release gates passed: formatting, Clippy, compilation, 94 default-feature
unit/async tests plus 6 CLI tests, 88 no-default-feature unit/async tests plus
6 CLI tests, system-scope, release-workflow, v1 contract, and shell syntax.

## GitHub-hosted systemd/TUN acceptance

The previous candidate run [30899326550](https://github.com/Pectics/mihoto/actions/runs/30899326550)
used the old self-hosted routing and was cancelled because the public
repository has no registered self-hosted runners. It produced no acceptance
evidence.

The workflow is now routed to GitHub-hosted Ubuntu 24.04 x86_64 and arm64
virtual machines, provisions a real stable Mihomo fixture, and will record the
systemd lifecycle, TUN interface and route behavior, DNS/controller check,
firewall state, rollback, service cleanup, and any hosted prerequisite failure.

## Publication boundary

Required from the tag workflow: four target archives, `SHA256SUMS`, GitHub
build provenance, and the final GitHub release publication.
