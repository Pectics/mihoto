# Mihoto v1.0.1 release audit

This record is updated from observed command and workflow results. It must be
complete before the `v1.0.1` tag is pushed; a placeholder or inherited
v1.0.0 result is not an acceptance result for this candidate.

## Release markers

Tested candidate commit: `PENDING_REAL_HOST_CANDIDATE_COMMIT`

Release tag: `v1.0.1`

Package version: `1.0.1`

Real-host acceptance: PENDING

Release decision: Pending real-host acceptance

## Automated gates

The final record will list the exact commit and result for formatting,
clippy, compilation, default-feature tests, no-default-feature tests, the
system-scope contract, the release-workflow contract, the v1 contract, and
the shell syntax checks.

## Physical-host acceptance

The x86_64 and aarch64 self-hosted systemd/TUN workflow runs are pending for
this candidate. Results will record the run URL, commit, host architecture,
systemd lifecycle, TUN interface and route behavior, DNS hijack behavior,
rollback, service cleanup, and any unavailable host prerequisite.

## Publication boundary

Required from the tag workflow: four target archives, `SHA256SUMS`, GitHub
build provenance, and the final GitHub release publication.
