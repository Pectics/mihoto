# Mihoto v1.0.1 release audit

This record is updated from observed command and workflow results. It must be
complete before the `v1.0.1` tag is pushed; a placeholder or inherited
v1.0.0 result is not an acceptance result for this candidate.

## Release markers

Tested candidate commit: `570ec46b92adec45ad824286e97727b42a7390f0`

Release tag: `v1.0.1`

Package version: `1.0.1`

Hosted systemd/TUN acceptance: PASS

Release decision: Accepted for v1.0.1 publication after hosted systemd/TUN acceptance

## Automated gates

For candidate commit `570ec46b92adec45ad824286e97727b42a7390f0`, the local
release gates passed: formatting, Clippy, compilation, 94 default-feature
unit/async tests plus 6 CLI tests, 88 no-default-feature unit/async tests plus
6 CLI tests, system-scope, release-workflow, v1 contract, and shell syntax.

## GitHub-hosted systemd/TUN acceptance

The previous candidate run [30899326550](https://github.com/Pectics/mihoto/actions/runs/30899326550)
used the old self-hosted routing and was cancelled because the public
repository has no registered self-hosted runners. It produced no acceptance
evidence. The first hosted attempt [30900579567](https://github.com/Pectics/mihoto/actions/runs/30900579567)
reached both hosted VMs but failed before acceptance because the decompressed
fixture lacked its executable bit; commit `570ec46` fixed that setup defect.

The workflow is now routed to GitHub-hosted Ubuntu 24.04 x86_64 and arm64
virtual machines and provisions Mihomo Meta v1.19.29 for both architectures.
Hosted acceptance run [30900661541](https://github.com/Pectics/mihoto/actions/runs/30900661541)
passed both jobs: x86_64 job `91963943084` passed systemd lifecycle, TUN
routing/cleanup, and all configured checks; aarch64 job `91963943120` passed
the same checks. The workflow emitted only the known checkout action Node.js
20 deprecation annotation; no acceptance step failed.

## Publication boundary

Required from the tag workflow: four target archives, `SHA256SUMS`, GitHub
build provenance, and the final GitHub release publication.
